# flask-devops-pipeline
A Flask app deployed with Docker and Kubernetes

## AWS Three-Tier Architecture

This repository includes a sample [AWS CloudFormation](https://aws.amazon.com/cloudformation/) template under `aws/three-tier-app.yaml`. It provisions a VPC with separate web, application, and database subnets along with EC2 instances for each tier to illustrate a basic three-tier deployment.
